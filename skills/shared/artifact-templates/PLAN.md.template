# {feature_title} - Implementation Plan

**Spec:** `docs/loop-spec/features/{slug}/SPEC.md`
**Created:** {created_at}

## Architecture overview

{2-3 sentences on approach.}

## System design

<!-- Record only changed facets. Greenfield covers all facets; otherwise use `- none`
     when the feature does not change the facet. -->
- Architecture / component structure: {owners and boundaries}
- Data flows / API design: {state flow and request/response shapes}
- Database schema / interface architecture: {changed entities and user surface}
- Caching strategy / scale bound: {invalidation and bounded growing input}

## Existing code

<!-- Look before you plan. For each concept this feature adds or changes, search the
     tree by domain words, follow imports and callers, and name the closest module that
     already does the job or something like it. One bullet per concept, one decision:
       reuse  - call that module through its current interface
       extend - put the new behavior behind that module's interface (deepen it)
       new    - nothing fits; name what you searched for
     reuse/extend cite the `path:lines` you read and the interface callers rely on
     (signature, invariants, error modes). Never copy code. A new module must pass the
     deletion test: deleting it would push its complexity back into several callers.
     Add a seam only where two adapters exist (production + test). Task blocks cite
     these entries in `read_first`. -->
- {concept}: {reuse|extend} `{path}:{lines}` — interface: {what callers rely on}; test analog: `{test path}`
- {concept}: new — searched {terms}; {why nothing fits}

## Global constraints

<!-- Binding rules every task must respect, stated VERBATIM (copied from SPEC
     boundaries/<decisions>, repo rules, or the tuning overlay) — never paraphrased.
     Execute dispatch briefs inline this section, so a wave implementer sees the
     rules without reading the spec. Keep `- none` if nothing binds globally. -->
- none

<!-- File map is optional; include it only for greenfield structure. Task blocks own
     changed-file detail. -->
## File map

- none

## Tasks

### task-001: {subject}

**Goal:** {one sentence naming the observable capability a caller or user gains}

**Files:**
- {path}

**read_first:**
- {path or path:line-range or path (section)}

**Interfaces:**
- consumes: {contract or none}
- produces: {contract or none}

**Verify:** `{exact command}` -> {expected output}

**Acceptance criteria:**
- [ ] {criterion with a named test or exact observable result}

**BlockedBy:** [{task ids; [] when none}]

**Steps:**

- [ ] {task-specific steps; include the slice's necessary layers and tests; code tasks
  state TDD red/green once}

(repeat task blocks; `lib/plan-tasks.sh extract` creates the JSON sidecar)

## Test strategy

{How tests will be run; per-task test commands rolled up.}

## Rollback plan

{How to revert if VERIFY phase fails after merges.}

## Grounding

<!-- Probe-before-assert (skills/shared/grounding-protocol.md): every load-bearing
     fact about an external system (dataset, API, service, infra) must cite an
     EVIDENCE.md entry (EVID-NNN) or be an explicit ASSUMPTION with a verify probe.
     Bullet grammar, one logical bullet per line (indent any wrapped continuation):
       - EVID-NNN: <what the probe confirmed>
       - ASSUMPTION: <claim> | verify: <read-only command>
     Keep the single `- none` line if nothing external is load-bearing. -->
- none
