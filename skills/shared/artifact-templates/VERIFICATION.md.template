# {feature_title} - Verification

**Spec:** `docs/loop-spec/features/{slug}/SPEC.md`
**Plan:** `docs/loop-spec/features/{slug}/PLAN.md`

## Repository grounding

One exact row per Good Enough criterion, identified by SPEC order as `GE-001`, `GE-002`,
and so on. Paths are repository-relative; workspace paths are relative to the workspace
root. `integration: none` requires a concrete reason.

- criterion: GE-001 | implementation: {path}:{line} - {what it proves} | integration: {path}:{line} - {what it proves}

## Acceptance criteria

<!-- v1 requirements contract: cycle-driver.sh verification run keys each row
     GE-ID/SC-ID (never a document-position number) and writes its Status and Evidence
     from a driver-owned observation record -- see agents/verifier.md, "v1 requirements
     contract". The legacy row shape below is unchanged. -->

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | {from SPEC} | PASS / FAIL / BLOCKED / N/A | `{verify command}` -> {output summary} |

<!-- BLOCKED: the check could not run for a reason outside the repository (expired
     credentials, an unreachable service, a missing approval). Name the reason and the
     operator action. Never write PASS for a check that did not run: the converged floor
     rejects PASS rows whose evidence says blocked, not run, skipped, or reauth. -->

## Verify command outputs

### Criterion 1

```
{full output of verify command}
```

(repeat per criterion)

## Code review

**Reviewer:** code-reviewer ({model})

### Findings

Accepted findings append `| routing: <JSON>` per `skills/shared/review-routing.md`.
One bullet per finding, or "none": `- <file>:<line> — <claim> | verdict: true — <commit,
backlog id, or fix>` for a finding you accepted, `- <file>:<line> — <claim> | verdict:
false — <disproof: what you ran or read that shows it wrong>` for one you rejected.
`lib/review-triage-lint.sh` at the exit rejects a bullet without `file:line`, without a
verdict, or a `false` without its disproof sentence.

#### Critical
{bullets or "none"}

#### Important
{bullets or "none"}

#### Minor (deferred)
{bullets or "none"; a Minor's verdict is `true — backlog <id>`}

#### Performance
{`perf:` bullets from the code-reviewer's performance pass, each with the input that
makes it cost and the measurement, or "none"}

### Resolution
{which findings were addressed in which commits, which deferred to follow-up tasks}

## Security review summary

{Any security-relevant findings.}

## Final test suite

```
{output of commands.test}
```

## Branch state

- Branch: `feat/{slug}`
- Final commit: {sha}
- Pushed: {y/n}
- PR URL: {url}
