# Evidence ledger
- EVID-001 | 2026-08-08T14:12:41Z | claim: lib/execute-rung.sh selects the EXECUTE rung from probed subagent, CLI and loop-runtime capability; all rungs are in-session | cmd: sed -n '34,45p' lib/execute-rung.sh | out: agent_cli="$(bash "$SCRIPT_DIR/harness.sh" cli)" loop_runtime="$(bash "$SCRIPT_DIR/harness.sh" loop-runtime)" loop_runtime_reason="$(bash "$SCRIPT_DIR/harness.sh" loop-runtime-reason)" cli_available="false" command -v "$agent_cli" >/dev/null 2>&1 && cli_available="true" loops_ava
- EVID-002 | 2026-08-08T14:12:41Z | claim: lib/dag-width.sh computes peak wave width by Kahn's algorithm and the EXECUTE ladder reads W to choose the rung | cmd: sed -n '4,13p' lib/dag-width.sh | out: # W is the peak number of tasks that become simultaneously runnable across a # topological wave simulation (Kahn's algorithm): repeatedly take every task # whose blockedBy edges are all satisfied, record the size of that ready set, # mark them done, repeat. W = max ready-set size
- EVID-003 | 2026-08-08T14:12:41Z | claim: feature.json is the committed resume contract, written atomically via lib/feature-write.sh; raw jq mutation is forbidden | cmd: sed -n '3,3p' skills/shared/feature-state-schema.md / head -c 280 | out: Per-feature runtime state lives at `.loop-spec/features/{slug}/feature.json`. It is the **committed resume contract** (tracked in git so resume survives a clone / hand-off; the cycle commits it on each phase transition). `PROGRESS.md` (the phase-transition journal) is committed a
- EVID-004 | 2026-08-08T14:12:41Z | claim: Hand-built inline feature.json previously dropped iterateJudge from the models map, which is why lib/feature-init.sh is the single source of truth | cmd: sed -n '563,568p' skills/cycle/SKILL.md | out: # Build the full schema-7 skeleton from the single source of truth (lib/feature-init.sh). # Model routes, configured phase defaults, the fixed iterate block, and the artifact scaffold all # live in that one script -- never hand-build feature.json inline (that drift is what # prev
- EVID-005 | 2026-08-08T14:12:41Z | claim: CLAUDE.md names the four converted probes and requires answer-plus-reason on one line, fail-safe on unknown, operator override outranking | cmd: grep -o 'lib/security-signal.sh\/lib/teams-capability.sh\/lib/harness.sh entrypoint\/lib/execute-rung.sh' CLAUDE.md / sort -u | out: lib/execute-rung.sh lib/harness.sh entrypoint lib/security-signal.sh lib/teams-capability.sh
- EVID-006 | 2026-08-08T14:12:41Z | claim: CLAUDE.md records graphify's removal in 2.35 with measured evidence: zero citations, zero refreshes, zero evidence entries | cmd: grep -o 'zero citations, zero recorded refreshes, and zero evidence entries' CLAUDE.md | out: zero citations, zero recorded refreshes, and zero evidence entries
- EVID-007 | 2026-08-08T14:12:41Z | claim: lib/events.sh states the observability contract that a telemetry failure must never abort a run | cmd: sed -n '4,7p' lib/events.sh | out: # OBSERVABILITY CONTRACT: this script must NEVER abort a cycle. All internal # failures print a one-line warning to stderr and exit 0. A broken telemetry # writer must not kill a 2-hour run. #
- EVID-008 | 2026-08-08T14:12:41Z | claim: lib/artifact-lint.sh lint_spec requires Problem, Success criteria, Good Enough with checkbox items, and Grounding | cmd: grep -n 'require_heading(display, lines, mask' lib/artifact-lint.sh / head -6 | out: 174:def require_heading(display, lines, mask, heading): 186:    require_heading(display, lines, mask, '## Problem') 187:    require_heading(display, lines, mask, '## Success criteria') 188:    ge = require_heading(display, lines, mask, '### Good Enough') 189:    require_heading(d
- EVID-009 | 2026-08-08T14:12:41Z | claim: lib/acceptance-lint.sh flags any acceptance criterion containing an unanchored grep | cmd: sed -n '36,45p' lib/acceptance-lint.sh | out:   exempt=0   case "$crit" in     *" -w"*/*"-w "*) exempt=1 ;;            # whole-word match     *'\b'*) exempt=1 ;;                      # word-boundary regex     *'grep -v'*) exempt=1 ;;                 # comment/line exclusion pipeline     *'grep -E'*'^'*) exempt=1 ;;          
- EVID-010 | 2026-08-08T14:12:41Z | claim: skills/shared/model-matrix.md binds model to role statically with no per-node effort input | cmd: sed -n '/^/ Role family/,/^/ mapper/p' skills/shared/model-matrix.md | out: / Role family / Model / /---/---/ / spec-writer, planner / opus / / challenger / opus / / iterate-judge / opus / / code-reviewer / opus / / advocate / sonnet / / spec-compliance-reviewer / sonnet / / verifier / sonnet / / implementer / sonnet / / mapper-*, pattern-mapper / sonnet
- EVID-011 | 2026-08-08T14:12:41Z | claim: PLAN's structural critique fast-path is at most 2 tasks and 3 files with no security signal, measured from the plan | cmd: grep -o 'at most 2 tasks and 3 files, no security signal, measured from the actual plan' README.md | out: at most 2 tasks and 3 files, no security signal, measured from the actual plan
- EVID-012 | 2026-08-08T14:12:41Z | claim: ROADMAP-3.0.md states 3.0 is not a breaking change to user surface and lists the carried-forward design constraints | cmd: sed -n '5,10p' docs/loop-spec/ROADMAP-3.0.md | out:  **Semver stance:** 3.0 is NOT a breaking change. Every 2.x invocation, artifact, config file, and hook keeps working unchanged. The major bump marks an identity change: the 2.x line is *a spec-driven cycle you invoke*; 3.0 is *a development agent that runs the loop itself, with 
