# 7.0 live runs

For a reviewer of the 7.0 pull request: which checklist case in
[ROADMAP-7.0.md section 17](ROADMAP-7.0.md#17-testing-live-gates-and-cutover)
was shown by which recorded run, and what evidence backs it. This page records
runs already made; it does not describe how to run one.

## Versions

| Component | Version |
|---|---|
| Claude Code | 2.1.278 |
| Model | sonnet |
| Python | 3.13.2 |

## Cases

| Case | Run tag | Result | Evidence | Notes |
|---|---|---|---|---|
| Every phase external, all seven boundaries (M1) | `m1-ext` (program driven from the operator's shell, no model) | `converged` | [live-runs/7.0/m1-ext](live-runs/7.0/m1-ext/) | PR live-7#1; ID-01 rejected a stale binding; E5 rejected a product with no review; T-1 `human-attested`. |
| Full native cycle: lead SPEC and PLAN, critic with a Critical and one re-run, implementer and reviewer as fresh workers, VERIFY re-runs, ITERATE, DELIVER | `m3-run1`..`m3-run5`, session `ba499f53-7a0b-4883-a32b-e913e5faa7c8` | `converged` | [live-runs/7.0/m3-divide](live-runs/7.0/m3-divide/) | PR live-7#2 at `4821812`, the exact verified SHA; review `host-attested`; AC-3 carried a PLAN-declared VERIFY exception (`weakenedAssurance`). LF-05 to LF-12 came from this run. |
| Attestation both directions: verbatim prompt accepted, paraphrased prompt rejected (AT-02) | same session as the row above | shown | `events.jsonl` in `m3-divide` (`step_submitted` evidence levels) | Review steps `step-667ca2a359a6` and `step-2f997b497d10` recorded `unattested` (the lead paraphrased the prompt; the attestor's reason was `opening does not contain the composed prompt`); the re-issued `step-6cdfad7be377` recorded `host-attested` and is the review the result names. |
| Blocked exit answered `stop` | `m2-run1`..`m2-run3`, session `8c31cb9d-573d-4c76-80fc-290908003ea4` | `escalated` | [live-runs/7.0/m2-multiply](live-runs/7.0/m2-multiply/) | Reason: `execute paused; operator chose 'stop'`; `phaseReached: execute`; no PR, no false completion. |
| Two-repo workspace: per-repo base, worktrees, reviews, PRs | `m5-ws4`..`m5-ws7`, session `5b0ac74d-de4d-4a1f-a2c6-f2aec4c0433c` | `converged` | [live-runs/7.0/m5-workspace](live-runs/7.0/m5-workspace/) | PRs live-7#3 (`calc`) and live-7b#1 (`textutil`), both tasks `host-attested`. LF-24, LF-28, LF-31 came from this run. |
| Program's own invalid product ends the run as `failed` | `m5-ws1`..`m5-ws3`, session `5979de5b-2bc7-4509-a7b2-af61f46ab661` | `failed` | [live-runs/7.0/m5-workspace-failed](live-runs/7.0/m5-workspace-failed/) | Reason names the field: `$.remediationTasks[0].id: does not match pattern ^T-[0-9]+$` at `verify`; fixed as LF-25 before the re-run above. |
| debug entry: reproduction at base, compact SPEC/PLAN, critic Critical and re-pass, `mustFlip` repair | `m4-debug1`..`m4-debug4`, sessions `0901e4a9…` then `8f6c43c6-8721-4019-ae17-9d7487325d30` | `converged` | [live-runs/7.0/m4-debug](live-runs/7.0/m4-debug/) | PR live-7#4 at the verified SHA `3a17411`; the repair task's re-run recorded `mustFlip-ok` ("reproduction no longer fails"); review, verifier, final review and iterate judge all `host-attested`. LF-32 to LF-36 came from this run. |
| Blocked at the retry limit answered `stop` (revise, before LF-38) | `m5-revise1`..`m5-revise2`, session `0cf12810-0a6c-4b2b-a974-3fe1a8fef2b3` | `escalated` | [live-runs/7.0/m5-revise-escalated](live-runs/7.0/m5-revise-escalated/) | EXECUTE re-dispatched the adopted PR's own task until its retries ran out; the run blocked, the operator answered `stop`. LF-37 to LF-41 came from this run. |
| Program's own invalid product in a revise run | `m5-revise3`..`m5-revise5`, session `fb8b435c-6470-4904-b985-10d08af6a988` | `failed` | [live-runs/7.0/m5-revise-failed](live-runs/7.0/m5-revise-failed/) | Reason names the field: `$.tasks[0].review: expected at least one anyOf branch to match` (LF-43); `worktrees_removed` fired on the terminal result (LF-39 live). |
| Rewind budget exhausted (T1) | `m5-revise6`..`m5-revise8`, session `40db2995-6948-4cac-b68d-b4fd75bdd1e4` | `escalated` | [live-runs/7.0/m5-revise-rewind-escalated](live-runs/7.0/m5-revise-rewind-escalated/) | VERIFY routed `plan gap` twice on a bare verifier flag with every verdict passing; the second rewind had no budget and the run escalated with `rewinds: 2` and the reason naming the budget. LF-44 and LF-45 came from this run. |
| revise entry: PR comments to gaps, prior SPEC/PLAN carried, adopted-range review, unchanged task adopted, new task implemented, PR updated | `m5-revise9`..`m5-revise10`, session `901bf8ed-338b-43ce-a298-95856d059eff` | `converged` | [live-runs/7.0/m5-revise](live-runs/7.0/m5-revise/) | PR live-7#2 updated to the verified SHA `6274539`; `task_adopted` for T-1 with its review `host-attested` through the adopted-range review; T-2 review `host-attested`; AC-3's PLAN-declared exception carried as `weakenedAssurance`; `rewinds: 0`. |
| Two independent tasks, critic-sequenced (both edit one file) | `m7-wave1`..`m7-wave2`, session `9b79281d-9dc8-40c6-babb-c81824fef218` | `converged` | [live-runs/7.0/m7-two-tasks](live-runs/7.0/m7-two-tasks/) | PR live-7#5 at `924c132`; the critic flagged the shared file and the lead sequenced T-2 after T-1, so the waves were single-task; a stale-binding PLAN submission was rejected once. |
| Wave dispatch: two disjoint tasks issued together, dispatched in one message, integrated with a merge commit; Minor finding deferred at ITERATE | `m7b-wave1`..`m7b-wave3`, session `4d697e21-1421-4996-984c-dc75a7f559c8` | `converged-with-caveats` | [live-runs/7.0/m7b-wave](live-runs/7.0/m7b-wave/) | PR live-7#6 at `5f4c588`, a two-parent merge commit (`loop-spec: integrate T-2`); two implementer and two reviewer steps issued per `LOOP_SPEC_NEXT` pairs and dispatched concurrently; the full-range review left one Minor finding, which the run first looped on (LF-46, LF-47) and, on the fixed program, deferred as a caveat (`finding_deferred`). |
| Per-wave review (7.0.7): one code-reviewer step for a wave's two tasks, after LF-71 | `waves-cli` on `482b3ae`, session `d1adfa9a-02b0-465c-9a22-802ebe8e5b34`, `claude -p --model sonnet`, `--answer-policy default` | `escalated` at DELIVER by policy (local remote), verification passed | [live-runs/7.0/wave-review](live-runs/7.0/wave-review/) | PLAN made two tasks on separate files in one wave (`[["T-1","T-2"]]`); EXECUTE issued one code-reviewer step, `step-c588f37a8241` (result `wave-T-1_T-2-review-*.json`, schema `tasks` minItems 2), `host-attested`, named in both tasks' `reviewSteps`; T-2 integrated with a two-parent merge; `rewinds: 0`, `weakenedAssurance` empty; 390 s, $1.88. The run before it on `2190d2e` was stopped in EXECUTE: the spec's "`uv run pytest -q` passes" criterion went uncovered (P2), and the lead covered it by making T-2 depend on T-1, so the waves were single-task and no wave review ran. That opened LF-71. |
| Repo checks (7.1.0): repoChecks facts to PLAN, PLAN `checks`, baseline, check at integration, V10 at VERIFY's head | `checks-cli` on `58050b1` (local fixture `unitkit`, uv, ruff + mypy strict in `pyproject.toml`), session `d6c4b64b-73fb-495c-844c-334b2ad29bea`, `claude -p --model sonnet`, `--answer-policy default` | `escalated` at DELIVER by policy (local remote), verification passed | [live-runs/7.0/repo-checks](live-runs/7.0/repo-checks/) | The PLAN step prompt carried `probes.repoChecks` with ruff and mypy (read from git objects at the base commit); the lead named `uv run ruff check --output-format concise` and `uv run mypy` as `checks`; the baseline ran both under the diagnostics parser (exit 0); the implementer prompt carried them; `executeCheckRuns` and `checkRuns` both record `no-regression` at the task head `5740381`, and VERIFY `passed` under V10. No check regressed, so the retry and remediation routes are shown by module tests only. 598 s, $1.81, `rewinds: 0`, review `host-attested`. |
| PLAN fact probes and verify checkout key (7.1.1): `probes.named` over the files the request names, read at the base; `verify-<repo>-<head12>-<prepare8>` | `checks-cli` on `4bcd831` (local fixture `unitkit`), session `e5041b48-8384-4ae4-9345-c59300d379bd`, `claude -p --model sonnet`, `--answer-policy default` | `escalated` at DELIVER (local remote: pushed `ead06c5`, no GitHub host for the PR), verification passed, ITERATE converged | [live-runs/7.0/plan-probes](live-runs/7.0/plan-probes/) | The planner prompt carried `probes.named.checks-cli` with `files` `tests/test_length.py` and `unitkit/length.py` (repo-relative) and their house style, duplication, indirection, security signals and deps (no signal, no third-party import in these files); no `plan-probes-*` checkout was left. VERIFY's checkout was `verify-checks-cli-ead06c5e1e2d-e3b0c442`. No file carried a security signal, so the disposition route and E11 are shown by module tests only. 526 s, $1.50, `rewinds: 0`. |
| Existing-code lookup (7.2.0): `existingCode` on the PLAN product, P8, the implementer's `existingCode` input | `checks-cli` on `6d6f05c` (local fixture `unitkit`), session `b26feb43-d75f-4571-9c8d-d52aaef57de6`, `claude -p --model sonnet`, `--answer-policy default` | `escalated` at DELIVER (local remote: pushed `4aa33cb`, no GitHub host for the PR), verification passed, ITERATE converged | [live-runs/7.0/existing-code](live-runs/7.0/existing-code/) | The planner recorded one `extend` entry for the yard/meter conversions citing `unitkit/length.py`. Its first PLAN cited lines `1-9` of an 8-line file and P8 rejected it (`product_rejected`: "cites unitkit/length.py:1-9, outside the file"); the resubmitted plan cited `1-8` and passed. The implementer prompt for T-1 carried the entry. No `new` entry, so the critic's duplicate-`new` Critical is module-test only. 434 s, $1.92. |
| DELIVER accepts a bot's CHANGELOG.md commit on the PR branch (`deliver.acceptRemotePaths`, 7.1.0) | `l2a` on `58050b1`, GitHub `loop-spec-live-7`, session `96592156-340b-4855-ae47-ae6070e04580`, no answer policy (SPEC approval and DELIVER's question answered by the operator, question-scoped) | `converged` | [live-runs/7.0/accepted-remote](live-runs/7.0/accepted-remote/) | With `deliver.readiness: "checks"` on a repo with no CI, DELIVER pushed `e087556`, opened PR live-7#22, failed D3 four times and asked. The operator pushed `bddadd9` (CHANGELOG.md only) onto the PR branch as a bot would, removed `readiness`, and answered fix-and-re-enter: DELIVER did not push (the remote stayed `bddadd9`), recorded `acceptedRemote` with that commit, kept `deliveredSha` `e087556`, and D1/D2 accepted the PR at `bddadd9`. `rewinds: 0`, `weakenedAssurance` empty. |
| DELIVER refuses a bot commit that touches the verified change | `l2b` on `58050b1`, GitHub `loop-spec-live-7`, session `aee69341-bad6-46c3-a7ed-9e065d6a8304`, same protocol | `escalated` (operator `stop`) | [live-runs/7.0/accepted-remote-refused](live-runs/7.0/accepted-remote-refused/) | PR live-7#23 at `ed7cbfc`; the operator pushed `70aa279` touching `calc/__init__.py`. On re-entry DELIVER exited `delivery blocked` naming `calc/__init__.py` and the repair, pushed nothing, and kept the earlier publication (`published: ed7cbfc`, PR #23) in the row. |
| Permission denial under the default mode | `m6-perm1`..`m6-perm3`, sessions `ffbdff4e…`, `2cbbafed…`, `09e10c5b…` | no result (the lead stopped) | the three run transcripts (not checked in; see the notes) | With only the launcher allow-listed, the lead stopped and asked for approval rather than widening permissions or claiming completion. Run 3 showed the default mode denying every write under `~/.claude/plugins/data`; LF-27 moved model-written results to the project's `.loop-spec/results/`. |
| Two-repo workspace on 7.0.0, stopped by the operator at VERIFY | `e2e-ws1`..`e2e-ws2`, session `428e74c5-96be-4649-a7aa-faf78bf56edd` | no result (stopped) | `events.jsonl` in [live-runs/7.0/e2e-workspace-lf49-51](live-runs/7.0/e2e-workspace-lf49-51/) | Per-repo baselines (R3) and the attestation refusal with re-dispatch behaved; an `implementation gap` rewind was never consumed by EXECUTE and a repeated ledger finding was re-minted and rejected by V8 on every submission. LF-49 to LF-52 came from this run (fixed in 7.0.1). |
| Two-repo workspace on 7.0.1, stopped by the operator at EXECUTE | `e2e-ws3`..`e2e-ws4`, session `bde86cc2-dc37-4973-992a-f9b8bd7a36f4` | no result (stopped) | `events.jsonl` in [live-runs/7.0/e2e-workspace-lf53](live-runs/7.0/e2e-workspace-lf53/) | Both implementers accepted on the first attempt; a retry worker's "already satisfied" claim was contradicted by its commits and took the review path (`already_satisfied_contradicted`, LF-49 live). A PLAN verify command joined with `&&` reached git as arguments (exit 128) on every retry; LF-53 came from this run (fixed in 7.0.2). |
| Two-repo workspace on 7.0.2: per-repo `featureAdded` baselines, two PRs, no rewind | `e2e-ws5`..`e2e-ws6`, session `88598ee2-f9a3-46a7-bba3-2e4b821cd004` | `converged-with-caveats` | [live-runs/7.0/e2e-workspace-7.0.2](live-runs/7.0/e2e-workspace-7.0.2/) | PRs live-7#7 (`calc`) and live-7b#2 (`textutil`), each touching only its task's two files; both verify commands were plain argv and recorded `no-baseline` per repo; `rewinds: 0`; one PLAN step re-dispatched once as `unattested`; one Minor finding deferred at ITERATE. |
| Headless answer policy, per-role model, critic question, ITERATE rewind loop | `e2e-c1`..`e2e-c2`, session `c2ff2574-1e80-4b07-a0fd-c8d850b5a808`, `LOOP_SPEC_MODEL_CODE_REVIEWER=haiku` | `escalated` | [live-runs/7.0/e2e-consumer-rewind-escalated](live-runs/7.0/e2e-consumer-rewind-escalated/) | `--answer-policy default` answered the requirements approval (`policyAnsweredQuestions`) with no relaunch; both code-reviewer steps carried `model: haiku` and no other role did. The request's `cd tests && ...` verify command was rewritten to plain argv by the lead before PLAN submitted, so LF-53's rejection did not fire. The PLAN critic raised a Critical judging a verify command by exit status (LF-54); the operator rejected it with a reason. ITERATE then rewound to EXECUTE twice on a free-text gap that EXECUTE had no task for, and VERIFY reused its cached runs, so the budget ran out with no change (LF-55). |
| Agent SDK reference supervisor, subscription auth | `sdk1`..`sdk2`, slug `add-is-even-n-to-calc-init-py-returning` | `converged` | [live-runs/7.0/sdk-supervisor](live-runs/7.0/sdk-supervisor/) | PR live-7#8; `claude-agent-sdk` 0.2.157 authenticated by a Claude subscription login (`apiKeySource: none`); every step `controller-observed`; a two-step EXECUTE wave dispatched from one call. `sdk1` stopped when the supervisor submitted a re-announced open step twice; fixed in the example, `sdk2` resumed the run from its slug. |
| Agent SDK session with loop-spec as a local plugin (`examples/sdk-plugin`) | `sdkp5`, session `66a8862f-9732-4f44-bbfd-c4319d64fb01`; `sdkp6` (`--entry micro`, after the `RunWatch` refactor), session `a1267dac-509b-4c52-847e-901ed398a181`, PR live-7#11, exit 0; earlier `sdkp1`..`sdkp4`, session `32a5ca45-edc1-4b30-8afa-e5cd6fb16a3d` | `converged` | [live-runs/7.0/sdk-plugin](live-runs/7.0/sdk-plugin/) (`sdkp5`), [live-runs/7.0/sdk-plugin-micro](live-runs/7.0/sdk-plugin-micro/) (`sdkp6`) | `sdkp5`: PR live-7#10 from a fresh start with no operator step, exit 0, 586 s; the requirements approval arrived as `AskUserQuestion` and `can_use_tool` answered it; reviews `host-attested`. `sdkp1`..`sdkp4` (PR live-7#9) found three ways the script ended early or waited forever: a turn ends while background workers run, a resumed session replays stopped tasks as a zero-turn result, and a finished task's notice can be handled inside the current turn. `RunWatch` in the example now owns that decision. `sdkp7` (session `7791fffa-d13b-4d8f-90f1-a93c4aa3c563`, no `--auto`, answer piped on stdin) answered the approval from stdin, then stopped in EXECUTE when one review step failed host attestation on all four dispatches (`opening does not contain the composed prompt`, LF-56); the example exited 2 with the resume hint. |
| revise entry on 7.0.2: one PR comment to gaps, critic question, PR updated | `e2e-t3b` on `0ffa2d2`, session `8be78b6b-c16c-4aef-8cba-08eed2a0a8e0`, slug `revise-6` | `converged` | [live-runs/7.0/e2e-revise-t3b](live-runs/7.0/e2e-revise-t3b/) | PR live-7#6 updated; T-1 to T-3 reviews `host-attested`; `rewinds: 1`. The PLAN critic's blocked question had no default and was answered by hand (LF-54). |
| PLAN critic judges a verify command on baseline facts (LF-54) | `e2e-lf54` on `b2c3379`, session `b211b140-7015-4706-bdd9-cb984d67e855` | `converged` | [live-runs/7.0/e2e-lf54](live-runs/7.0/e2e-lf54/) | PR live-7#12; the critic raised no finding against a suite with a known failing test; `rewinds: 0`. The ITERATE judge step `step-50b2e560b9b1` failed attestation on four dispatches and was accepted under the old auto-waiver (`weakenedAssurance`). The cause was escaped non-ASCII text in its prompt (LF-57); LF-60 later removed the auto-waiver. |
| Two-repo delivery with one push rejected, before LF-58 | `e2e-t2b` on `b2c3379`, session `3d63c751-62c0-445f-86bf-8347549aba8a` | `escalated` | [live-runs/7.0/e2e-t2b-partial-delivery](live-runs/7.0/e2e-t2b-partial-delivery/) | EXECUTE reviews `host-attested` (LF-56 live). `textutil`'s push URL pointed nowhere. DELIVER paused the whole delivery on that one rejection; the operator answered `stop`. `calc` had already published PR live-7#13, yet the result said `partiallyDelivered: false`. Both defects became LF-58. The ITERATE judge was again accepted after four unattested dispatches (LF-57). |
| Non-ASCII prompt text attests (LF-57) | `e2e-lf55` on `36ae40e`, session `c93cdd81-7b41-46c9-9450-e1c50c1c87ba` | `converged` | [live-runs/7.0/e2e-lf55](live-runs/7.0/e2e-lf55/) | PR live-7#14; `rewinds: 0`; `weakenedAssurance` empty. Every judgment step attested without the waiver the two rows above needed. |
| Instruction-file transport (LF-59) and an ITERATE close-out in one consumer run | `e2e-lf59` on `311d67f`, session `5533e74a-dc1a-442a-a7db-76f4b32fb963` | `converged-with-caveats` | [live-runs/7.0/e2e-lf59](live-runs/7.0/e2e-lf59/) | PR live-7#15; `rewinds: 1`; `weakenedAssurance` empty. Every role step ran on `transport: file` and was `host-attested` on its first dispatch: critic, four reviews, two judges. The two largest review prompts ran to 1,623 and 1,651 lines, yet each worker read its file with one Read. This run supports the single-read path only; it does not close the large-input gate (see the chunked-read row). |
| ITERATE rewind to EXECUTE closed by a close-out task (LF-55) | `e2e-lf55b` on `311d67f`, session `1ce3ffc8-c62b-4e79-91a8-e455ab0edb39`, ITERATE bound `external` | `converged` | [live-runs/7.0/e2e-lf55b](live-runs/7.0/e2e-lf55b/) | The operator's ITERATE product rewound with one `execute` gap on `calc`; the controller registered `C-1`. EXECUTE implemented and reviewed it (`host-attested`, file transport), and VERIFY re-ran at the new head. The second ITERATE product converged. `C-1` closed with commit `75ee92f`, which PR live-7#16 carries; `rewinds: 1`. |
| Two-repo delivery with one push rejected, after LF-58 (handoff test 2) | `e2e-t2c` on `4d9f08e`, session `fc3f97d0-3d23-4e07-a145-f029b451172a` | `escalated` | [live-runs/7.0/e2e-t2c-partial-delivery](live-runs/7.0/e2e-t2c-partial-delivery/) | `textutil`'s push URL again pointed nowhere. DELIVER exited `partially delivered`: `calc` delivered PR live-7#17 and `textutil` failed. Its caveat carries git's raw stderr and the general repair ("check origin's push URL, network, and access"). The result reads `escalated`, `partiallyDelivered: true`, reason `did not deliver: textutil`. Every judgment step was `host-attested`; `weakenedAssurance` is empty. |
| Chunked instruction-file read of a large review prompt (LF-59), stopped by the operator | `e2e-lf59b` on `4d9f08e`, session `0fa27f6d-d446-480b-bc7f-c0f725e3347e` | no result (stopped) | `events.jsonl` in [live-runs/7.0/e2e-lf59b-chunked](live-runs/7.0/e2e-lf59b-chunked/) | The T-1 review prompt was 1,645 lines of mostly sha256 hex. No review worker read every line before other work, so eleven dispatches over three review steps all failed attestation. The first Read stopped silently at line 537 on the host's 25,000-token cap. A follow-up Read of 537 lines was refused as over the cap. Workers then skipped ahead or ran `wc -l` in Bash. The attestor refused each receipt correctly. On this pre-LF-60 checkout, the two exhausted review steps were still auto-waived, and E6 then rejected them. This run opened LF-61: the program should give the worker a read schedule. |
| Scheduled reads of large prompts (LF-61), ITERATE diffs as real lines (LF-63), critic question answered by policy (LF-62) | `e2e-lf59c` and `e2e-lf59c-r1` on `c68fae2`, then `e2e-lf59c-r2` on `ffcd87a`, session `9bb9f810-ddf0-408c-8a5a-9a31ce5ddad4` | `escalated` | [live-runs/7.0/e2e-lf59c-scheduled-read](live-runs/7.0/e2e-lf59c-scheduled-read/) | Every plan-critic, code-reviewer and iterate-judge step was `host-attested` on its first dispatch. Four prompts were large: the two EXECUTE reviews (1,629 and 1,615 lines) and the two ITERATE judges (1,589 and 1,632 lines). Each was read in its 7 scheduled Reads before any other tool. `weakenedAssurance` and `refused` are empty. On `c68fae2`, the first judge step was never issued, because one prompt line was 94,463 bytes (LF-63); the run resumed on `ffcd87a`. The operator answered the first second-pass critic question by hand (`by: human`). The second ITERATE rewound to PLAN, and the critic raised F-1 again. This time the policy answered with the critic's recommendation, `spec gap` (`by: policy`, the LF-62 path live). With `rewinds: 2` spent, the run escalated: `the rewind budget has no room for plan spec gap`. The work was verified but not delivered, so this is not a convergence. |
| LF-60 refusal: a plan-critic step with a wrong opening is refused and stops the run | `e2e-lf60b` on `ffcd87a`, session `4367451f-fc63-4ad0-aebd-bdbbd1bbc642`, `LOOP_SPEC_STEP_RETRIES=1`; resumed as `e2e-lf60b-r1` | `converged` (after the resume) | [live-runs/7.0/e2e-lf60b-refusal](live-runs/7.0/e2e-lf60b-refusal/) | A maintainer probe in the lead's system prompt had it prefix `OPERATOR PROBE LINE` to both plan-critic dispatches of `step-cb9564f8f9ff`. Each was unattested (`opening is not exactly the step's dispatch prompt`). After the second, the program refused the step: one `step_refused` event, `refused` recorded with `ownerReset: true`, `criticStepId` cleared, and no critic recorded. It then asked `question-8c6bad8ccfee`, with `fix-and-re-enter` and `stop` and no default, and the headless policy stopped there. The operator answered `fix-and-re-enter`. That issued a fresh critic step, `step-1287ddf6c61c`, which the resumed lead dispatched exactly (`host-attested`). The run delivered PR live-7#19, with `weakenedAssurance` empty. |
| LF-60 opt-in: an unattested plan-critic accepted under `evidence.judgment.accept`, carried to the result | `lf60-optin-shell` on `ffcd87a` (operator shell, no model, through PLAN), then `e2e-lf60-optin-finish`, session `ca543d1d-dc57-4063-8359-a0cf071fdf9d` | `converged` | [live-runs/7.0/lf60-optin-shell](live-runs/7.0/lf60-optin-shell/) | Driven like `m1-ext`: the operator wrote the SPEC and PLAN lead products and a no-findings critic result, then submitted with no `CLAUDE_CODE_SESSION_ID`, so no host could attest. With `evidence.judgment.accept: "unattested"` set, the critic `step-1d0b170ade4e` was accepted `unattested`, and PLAN advanced to EXECUTE. It recorded `{kind: "evidence.unattested-step", step, role: "plan-critic", attempts: 1, policy: "evidence.judgment.accept", source: "config"}`. Model leads declined to fake this case twice (the row below), so it was shown without one. A normal model lead then finished the run with the same config, dispatching every step exactly (all `host-attested`). The run delivered PR live-7#21 with `rewinds: 0`, and the result's `weakenedAssurance` carries that waiver unchanged, with its `policy` and `source`. |
| Whole cycles on `ffcd87a` with no probe applied | `e2e-lf60`, session `66237a3e-c8ba-4f5d-abc5-61de67fbfe54`; `e2e-lf60c`, session `1de712ea-cf25-463c-a871-0d789c1e03f1` | `converged` | [live-runs/7.0/e2e-lf60-clean](live-runs/7.0/e2e-lf60-clean/), [live-runs/7.0/e2e-lf60c-declined-probe](live-runs/7.0/e2e-lf60c-declined-probe/) | Both leads were asked to prefix the plan-critic dispatch: first in the request text, then in the system prompt for the opt-in case. Both declined, saying it would break the step's attestation, and dispatched every step exactly. Each run converged with `rewinds: 0` and empty `weakenedAssurance`: PR live-7#18 and PR live-7#20. |

## Timing: FastAPI services, `claude -p` and the Agent SDK

Three requests, each a uv + Python 3.14 FastAPI service with pytest tests: `todo`
(in-memory CRUD), `shortener` (SQLite), `inventory` (pagination, stock adjust with
409). Each ran once under `claude -p --model sonnet` (Claude Code 2.1.280) and once
under [`examples/sdk-plugin`](../../examples/sdk-plugin/) with `--model sonnet --auto`
(claude-agent-sdk 0.2.157, bundled CLI 2.1.277), both headless with
`--answer-policy default`, two runs at a time. Each fixture repository's `origin` is a
local bare repository, so DELIVER always pushes and then stops at the PR step
(`delivery blocked`); DELIVER itself takes seconds, and the operator's `stop`
(by hand on 7.0.3, by policy on 7.0.4) turns it into the terminal `escalated`.
Wall time is the launched process, start to exit. Evidence:
[live-runs/7.0/fastapi-timing](live-runs/7.0/fastapi-timing/).

| Run | Plugin | Wall | Cost | Turns | Rewinds | End |
|---|---|---|---|---|---|---|
| `todo-cli` | 7.0.3 `58639ed` | 1909 s | $6.29 | 75 | 0 | no result: LF-64 loop, the lead fixed the code outside any step |
| `todo-sdk` | 7.0.3 `58639ed` | 1717 s | $8.65 | 110 | 0 | no result: LF-64 loop, then the LF-65 pause re-asked five times |
| `shortener-cli` | 7.0.3 `58639ed` | 1173 s | $4.23 | 43 | 0 | `escalated` at DELIVER, verification passed |
| `shortener-sdk` | 7.0.3 `58639ed` | 1279 s | $4.97 | 63 | 0 | `escalated` at DELIVER, verification passed |
| `inventory-cli` | 7.0.3 `58639ed` | 2124 s | $6.52 | 84 | 1 | `escalated` at DELIVER, verification passed |
| `inventory-sdk` | 7.0.3 `58639ed` | 957 s | $3.58 | 60 | 0 | `escalated` at DELIVER, verification passed |
| `todo2-cli` | 7.0.4 `db0c744` | 1971 s | $8.12 | 93 | 1 | `escalated` at DELIVER by policy, verification passed |
| `todo2-sdk` | 7.0.4 `db0c744` | 1094 s | $3.63 | 46 | 0 | `escalated` at DELIVER by policy, verification passed |
| `todo3-cli` | 7.0.5 `ff43731` | 686 s | $2.82 | 52 | 0 | `escalated` at DELIVER by policy, verification passed |
| `todo3-sdk` | 7.0.5 `ff43731` | 1755 s | $4.28 | 56 | 0 | `escalated` at DELIVER by policy, verification passed |
| `shortener3-cli` | 7.0.5 `ff43731` | 885 s | $3.60 | 71 | 0 | `escalated` at DELIVER by policy, verification passed |
| `shortener3-sdk` | 7.0.5 `ff43731` | 1076 s | $3.91 | 59 | 1 | `escalated` at DELIVER by policy, verification passed |
| `inventory3-cli` | 7.0.5 `ff43731` | 1598 s | $5.51 | 68 | 2 | `escalated` at DELIVER by policy, verification passed |
| `inventory3-sdk` | 7.0.5 `ff43731` | 903 s | $3.13 | 54 | 0 | `escalated` at DELIVER by policy, verification passed |

A run with no rewind spent about 40 s in SPEC, 180 to 230 s in PLAN, 360 to 560 s in
EXECUTE, 200 to 290 s in VERIFY and 80 to 120 s in ITERATE. Both rewinds were ITERATE
forcing an open Important finding (a thread-pool race in the in-memory store) to
PLAN, which then took 8 to 12 minutes: a re-plan the program rejected twice (criteria
dropped, a new verify command with no baseline run) and a critic that re-raised a
finding its own recommendation closed. The `todo` pair on 7.0.3 opened LF-64 to LF-66;
on 7.0.4 the DELIVER question was answered `stop` by policy (LF-66 live). Neither
7.0.4 review left a Critical open, so LF-64's route is shown by module tests only.
`todo2-cli`'s lead once ran `cycle --request resume` instead of `--slug`, which started
a stray run named `resume` that it abandoned after SPEC issued one step.

On 7.0.5 every run reached its terminal result with no operator input (the policy
answered the SPEC approval and DELIVER's `stop`), and every role step was
`host-attested` on its first dispatch from `dispatch.txt`. Against 7.0.3, the four
runs of the two tasks that finished there took 4462 s and $16.15 in total, down from
5533 s and $19.30. `inventory3-cli` showed both new routes live: VERIFY sent an open
Critical to EXECUTE as remediation `R-12` (LF-64), and ITERATE's gap (a pagination
default an approved decision required) closed as close-out `C-1` in about two minutes
(LF-68's route), with no re-plan. `shortener3-sdk`'s rewind was also an ITERATE
close-out. `todo3-sdk`'s planner chained config, app and tests into three one-task
waves, and its EXECUTE took 20 minutes against 5 for `todo3-cli`'s two-task plan;
that opened LF-70 (7.0.6).

## Reading the evidence

Each evidence directory holds the run's `result.json` (the shape `schemas/result.json`
in the program declares) and its `events.jsonl`. Paths inside them are the maintainer's
scratchpad at run time and are kept verbatim. The run tags name the `claude -p`
launches of one session; a session was resumed after each fix so the case continued
on the fixed program. An evidence directory with a `program-commit.txt` names the
plugin checkout that run read; the Run tag column repeats it as `on <sha>`.
