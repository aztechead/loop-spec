---
trust: generated
---

# TECH

> **Generated snapshot, not a runtime contract.** This map describes the tree at its
> recorded generation date and may retain facts from an earlier release. For current
> harness and model behavior, use [the architecture guide](../gdd.md),
> [model matrix](../../../skills/shared/model-matrix.md), and
> [model policy](../../../skills/shared/model-policy.md); regenerate the map before
> using it to plan changes.

> Generated by loop-spec-mapper-tech on 2026-08-06. Mode: full. Supersedes the
> 2026-05-11 revision, which had drifted: it described `tests/smoke.sh` (later
> `tests/e2e/run-e2e.sh`, since removed — the test tree is offline-only),
> `agents/loop-spec-*.md` (agent files carry bare role
> names, no prefix — `CLAUDE.md` "Adding an Agent"), `skills/shared/preset-matrix.md`
> (now `skills/shared/model-matrix.md` + `model-policy.md`), and model IDs
> `claude-opus-4-7` / `claude-haiku-4-5` (current resolution below). Every claim
> in this revision was re-checked against the file cited beside it.

## Languages

| Language | Role | File count (repo tree, excluding `.git/`) |
|---|---|---|
| Markdown (`.md`) | Primary artifact format: skills, agents, commands, docs, state schemas | 154 |
| Bash (`.sh`) | Nearly all runtime logic: `lib/` helpers, hooks, test suites | 260 |
| JavaScript (`.js`) | Claude Code `Workflow` tool scripts (`lib/workflows/*.js`, `.claude/workflows/*.js`) — ES-module dialect executed by the Claude Code Workflow runtime, not by a project Node build | 9 |
| Python (`.py`) | `skills/loop-runner/scripts/` (compile_spec.py, loop.py, planlib.py, supervisor.py) — the loop-runner engine — plus its `skills/loop-runner/tests/config_flags.py` fixture | 5 |
| TypeScript (`.ts`) | `extensions/pi/loop-spec.ts`, `extensions/opencode/loop-spec.ts` — harness bridge extensions (CLAUDE.md philosophy: node builtins only, never npm/bun) | 2 |
| JSON (`.json`) | Plugin/marketplace manifests, hooks config, pi manifest, state files | 7 (repo-tracked, excludes generated `.loop-spec/` run state beyond the one checked-in `index.json`) |
| YAML (`.yml`) | One example recipe, `docs/examples/issue-to-pr.yml` | 1 |

Counted with `find . -name '*.<ext>' -not -path './.git/*' \| wc -l` on this run. No compiled languages (Go, Rust, Java, C/C++). No Ruby.

## Package Manager

None for shipped runtime code, by design (`CLAUDE.md` "Lean deps"). There is a `package.json` at the repo root (`package.json:1-31`), but it is the **pi package manifest** (`pi.skills`, `pi.prompts`, `pi.extensions` keys), not an npm dependency manifest — it declares zero `dependencies`/`devDependencies` fields. No `requirements.txt`, `pyproject.toml`, `Cargo.toml`, `go.mod`, or `Gemfile` exist anywhere in the tree.

The `.ts` harness bridges (`extensions/pi/loop-spec.ts`, `extensions/opencode/loop-spec.ts`) import only `node:child_process`, `node:path`, `node:url`, `node:fs`, `node:crypto` (verified: `grep -n "^import" extensions/{pi,opencode}/loop-spec.ts`) — no npm install step, no `node_modules/`, no lockfile (`package-lock.json` / `pnpm-lock.yaml` / `bun.lockb` absent from the tree).

## Production Dependencies

None declared in a manifest — this project ships no compiled/installed package, only markdown+bash+jq+python3 source consumed directly by a host harness. What the shipped code invokes at runtime:

| Tool | Usage | Where cited |
|---|---|---|
| `bash` | Every `.sh` file (`#!/usr/bin/env bash`, `set -euo pipefail`) | e.g. `tests/run-all.sh:1,8` |
| `git` | Branch/worktree management, commit ops | `lib/git-ops.sh` |
| `jq` | JSON validation and querying throughout `lib/*.sh` | e.g. `lib/feature-write.sh`; floor pinned in `lib/runtime-preflight.sh` |
| `python3` | JSON parsing in `hooks/restrict-agent-paths.sh` (stdlib `json` only); the full loop-runner engine (`skills/loop-runner/scripts/{loop,compile_spec,planlib,supervisor}.py`) | `docs/loop-spec/PREREQUISITES.md:5` |
| `claude` (CLI) | Plugin host under the Claude Code harness; headless dispatch target for loop-fleet rungs | `lib/harness.sh:103-109` |
| `pi` (CLI) | Headless dispatch target for loop-fleet rungs under the pi harness | `lib/harness.sh:103-109`, `README.md:48` |
| `opencode` (CLI) | Headless dispatch target for loop-fleet rungs under the opencode harness | `lib/harness.sh:103-109`, `README.md:65-71` |
| `gh` (GitHub CLI) | PR creation/lookup/comments for the deliver phase (`lib/checkpoint-pr.sh`, `lib/pr-comments.sh`, `lib/pr-delivery.sh`) | `lib/pr-comments.sh:201-227`, `lib/pr-delivery.sh:311-491` |
| `node` | Executes the Claude Code Workflow scripts at runtime (harness-provided, not project-installed) | `lib/workflow-availability.sh` gates on Claude Code version, not on a local node install |

`lib/feature-write.sh` (atomic `feature.json` writes with `.bak` rotation) and `lib/team-ops.sh` (agent-teams helpers) still exist and are current — verified present at `lib/feature-write.sh` and `lib/team-ops.sh`. The old TECH's `lib/state-write.sh` "replaced by" note is moot: `lib/state-write.sh` does not exist in the tree (confirmed absent).

## Runtime Requirements

Declared floors, `docs/loop-spec/PREREQUISITES.md:5` and `CLAUDE.md` ("Philosophy", first bullet), identical in both:

| Requirement | Floor | Source |
|---|---|---|
| `bash` | `>= 4` | `docs/loop-spec/PREREQUISITES.md:5` |
| `git` | no version floor stated | `docs/loop-spec/PREREQUISITES.md:5` |
| `jq` | `>= 1.5` | `docs/loop-spec/PREREQUISITES.md:5` |
| `python3` | `>= 3.6` (declared) | `docs/loop-spec/PREREQUISITES.md:5` |

**Discrepancy found:** the loop-runner engine imports `dataclasses` unconditionally (`skills/loop-runner/scripts/loop.py:43` — `from dataclasses import dataclass, asdict, field`; also `supervisor.py`). The `dataclasses` module was added to the Python standard library in **3.7**, not 3.6 — so the actual floor for loop-runner is `python3 >= 3.7`, one minor version above the declared `>= 3.6`. The declared floor is honest for the rest of the codebase (plain f-strings, no other 3.7+-only stdlib usage found), but it does not hold for `skills/loop-runner/scripts/*.py` as written.

Minimal Linux images (Alpine/distroless) additionally need `apk add jq python3` (`CLAUDE.md`, "Lean deps"; `docs/loop-spec/PREREQUISITES.md:35`).

Additional, optional, harness/mode-specific requirements:

| Requirement | Detail | Source |
|---|---|---|
| Claude Code `>= v2.1.32` | Minimum for agent teams (optional accelerator; absent it, the no-teams fallback runs the full cycle end to end) | `docs/loop-spec/PREREQUISITES.md:30-59` |
| `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` | Opt-in flag for agent teams; unset → `teamsMode=none`, one-shot `Agent` dispatch, no abort | `docs/loop-spec/PREREQUISITES.md:10-53` |
| Claude Code `>= 2.1.154` | Minimum for the `Workflow` tool (`lib/workflow-availability.sh:19` `MIN="2.1.154"`); absent, EXECUTE falls back to bounded one-shot waves | `lib/workflow-availability.sh:2-19` |
| Claude Code `>= 2.1.178` | Boundary between the explicit (`TeamCreate`/`TeamDelete`) and implicit (`Agent({name})`) agent-teams tool generations; `lib/teams-capability.sh` gates on it | `docs/loop-spec/PREREQUISITES.md:38-50` |
| `gh auth status` (authenticated) + an `origin` remote | Required for prompt-to-PR delivery only | `docs/loop-spec/PREREQUISITES.md:35` |
| `pi` CLI | Required only under the pi harness, for the loop-fleet rung | `docs/loop-spec/PREREQUISITES.md:121-123` |
| `node` (any resolvable binary) | Required only to syntax-check `lib/workflows/*.js` in the test suite (`tests/workflows/smoke.sh`); its absence skips that one suite without failing `tests/run-all.sh` | `tests/run-all.sh:179-188` |

Verified present in this execution environment (not a project requirement, just this run's facts): `python3` 3.11.15, `jq` 1.7, `git` 2.43.0, `bash` 5.2.21, `node` v22.22.2.

Under pi, the Claude Code-only surfaces above (agent teams, the Workflow tool) are hard-gated off regardless of environment variables (`lib/teams-capability.sh`, `lib/workflow-availability.sh:28-40`); pi's prerequisites are just the base runtime plus the `pi` CLI (`docs/loop-spec/PREREQUISITES.md:116-123`).

## Dev Dependencies

None declared in a manifest. All test tooling is bash- and python3-stdlib-only:

- `skills/loop-runner/tests/run_tests.sh` uses `tests/fakeclaude`, `tests/fakeopencode`, `tests/fakepi` — project-authored stand-in scripts, not installed packages, so `loop.py`/`supervisor.py` can be regression-tested without a real `claude`/`opencode`/`pi` CLI or network access (`skills/loop-runner/tests/run_tests.sh:1-20`; fixture scripts confirmed present at `skills/loop-runner/tests/fakeclaude`, `fakeopencode`, `fakepi`).
- `pytest` appears only as **string literals inside test fixtures** that exercise `lib/detect-test-cmd.sh`'s ability to recognize a *target* project's test command (e.g. `tests/lib/detect-test-cmd.test.sh:59-98`, `tests/lib/prepare-environment.test.sh:66`) — it is not a dependency of loop-spec itself, and no fixture project (e.g. the old TECH's `tests/fixtures/minimal-py/`) exists in the current tree to be a `pytest` dependent (confirmed: `find tests/fixtures -iname '*calculator*' -o -iname '*pytest*' -o -iname Makefile` returns nothing).

## Tools

| Tool | Purpose | Source |
|---|---|---|
| `bash tests/run-all.sh` | Meta test runner: ~150 suites — validators, hooks, `lib/` units, `tests/*-coverage.test.sh`, workflow syntax check, `skills/loop-runner/tests/run_tests.sh` | `tests/run-all.sh:39-177` |
| `bash tests/validate-agents.sh` | Validates all agent `.md` files: frontmatter, name-matches-filename, `tools`/`model` present, restricted roles lack Write/Edit | `tests/run-all.sh:39` |
| `bash tests/workflows/smoke.sh` | Syntax-checks `lib/workflows/*.js` by emulating the Workflow runtime's ESM wrapper; needs a resolvable `node` (falls back to a pinned nvm path, `$HOME/.nvm/versions/node/v22.14.0/bin/node`), skipped (not failed) otherwise | `tests/run-all.sh:179-188`, `tests/workflows/smoke.sh:1-40` |
| `bash skills/loop-runner/tests/run_tests.sh` | Offline regression suite for the loop-runner Python engine, using `fakeclaude`/etc. | `tests/run-all.sh:177` |

No linter, formatter, or type checker is configured for this repository (no `.shellcheckrc`, `.eslintrc*`, `pyproject.toml`, or `setup.cfg` found anywhere in the tree — confirmed by direct search). No CI config exists in the repo (no `.github/workflows/`) — `tests/run-all.sh` is invoked manually or via an external/undeclared recipe.

## Build / Run Commands

| Command | What it does | Source |
|---|---|---|
| `bash tests/run-all.sh` | Run all offline suites (no `claude` CLI required; the test tree is offline-only) | `tests/run-all.sh:2-7` |
| `claude plugin marketplace add https://github.com/aztechead/loop-spec.git` | Register the plugin marketplace (Claude Code) | `README.md:31` |
| `claude plugin install loop-spec@loop-spec-marketplace` | Install the plugin into Claude Code | `README.md:32` |
| `pi install git:github.com/aztechead/loop-spec` | Install as a pi package | `README.md:48` |
| `bash lib/opencode-install.sh install` | Install globally under opencode (`~/.config/opencode`) | `README.md:67` |
| `bash lib/opencode-install.sh install --project .` | Install per-project under opencode (`./.opencode`) | `README.md:68` |
| `bash lib/bump-version.sh <version>` | Set the version in all four declaration sites (`package.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, README line) at once | `CLAUDE.md` ("Philosophy", "Multi harness"); confirmed at `lib/bump-version.sh` |
| `bash lib/bump-version.sh --check` | Report version drift across those four sites without changing anything | `CLAUDE.md` ("Philosophy", "Multi harness") |

Current version across all four declaration sites, verified consistent: **2.35.0** (`package.json:3`, `.claude-plugin/plugin.json:5`, `.claude-plugin/marketplace.json:12`, `CHANGELOG.md:5` `## [2.35.0] - 2026-08-06`, `README.md:22`). The old TECH's "as of v1.0.0" header no longer reflects the shipped version.

## Plugin / Integration Points

| Integration | Details | Source |
|---|---|---|
| **Claude Code plugin system** | Registered via `.claude-plugin/plugin.json` (version 2.35.0). Skills in `skills/*/SKILL.md` (35 skill directories), slash commands in `commands/*.md`, agent definitions in `agents/{role}.md` — **bare role names, no `loop-spec-` prefix**; the harness namespaces the caller to `loop-spec:{role}` (`CLAUDE.md`, "Adding an Agent") — hooks in `hooks/hooks.json` | `.claude-plugin/plugin.json:1-22`; agent filenames confirmed via `find agents -name '*.md'` (16 role files + `README.md`) |
| **Claude Code hooks** | `hooks/hooks.json` registers hooks across `SessionStart`, `PreToolUse` (matchers: `AskUserQuestion`; `Skill`; `Agent\|Bash\|EnterWorktree`; `Write\|Edit`; `TaskCreate`; `TaskUpdate`), `PostToolUse`, `UserPromptSubmit`, `TaskCompleted`, `TeammateIdle`, and `Stop`. `hooks/restrict-agent-paths.sh` enforces path-scoped write restrictions per subagent type from the session transcript JSONL. `hooks/team/placeholder-question-guard.sh` denies dummy wait questions. Agent-teams event hooks live in `hooks/team/` | `hooks/hooks.json` |
| **Claude Code Workflow tool** | `lib/workflows/*.js` (5 workflow scripts: map-codebase, execute-dag, code-review-dimensions, acceptance-verify, plan-multi-angle) plus shared `templates/*.snippet.js`; installed into `.claude/workflows/` by `hooks/install-bundled-workflows.sh`. Gated on Claude Code `>= 2.1.154` and the `claude` harness only | `lib/workflow-availability.sh:2-40`; `hooks/install-bundled-workflows.sh` |
| **pi extension** | `extensions/pi/loop-spec.ts` — node-builtins-only bridge exporting `LOOP_SPEC_HARNESS=pi`, `CLAUDE_PLUGIN_ROOT`, `CLAUDE_PROJECT_DIR`, `CLAUDE_SKILL_DIR` into every bash command; runs SessionStart/UserPromptSubmit/session-end equivalents | `extensions/pi/loop-spec.ts:1-52`; `package.json:27-29` |
| **opencode plugin** | `extensions/opencode/loop-spec.ts` — same bridge role for opencode via documented plugin hooks (`shell.env`, `chat.message`, event stream); installer at `lib/opencode-install.sh` generates namespaced `loop-spec-<name>` skill/agent adapters and `/loop-spec/<name>` commands | `extensions/opencode/loop-spec.ts:1-30`; `README.md:65-71` |
| **Anthropic model API (via the `claude`/`pi`/`opencode` CLIs)** | Model routing is by alias (`sonnet \| opus \| haiku \| fable`), not literal ID — the Agent tool's `model` parameter rejects literal IDs with `InputValidationError`. As of `skills/shared/model-matrix.md`'s last update: `opus -> claude-opus-4-8`, `sonnet -> claude-sonnet-4-6`. Selection governed by `skills/shared/model-matrix.md` and `skills/shared/model-policy.md` (the old TECH's `skills/shared/preset-matrix.md` no longer exists) | `skills/shared/model-matrix.md:1-18` |
| **GSD (get-shit-done) ingest** | Optional. If the target project has `.planning/codebase/`, `lib/gsd-ingest.sh` concatenates GSD artifacts into loop-spec format at first-run setup. No dependency at dev time | `lib/gsd-ingest.sh` |
| **git** | Worktree-based parallel execution (per-task worktrees under `.loop-spec/worktrees/{slug}/task-NNN/`), feature branches (`feat/{slug}`), atomic state with SHA tracking; `LOOP_SPEC_WORKTREES=0` opts into an in-place branch with serial EXECUTE instead | `lib/git-ops.sh`; `docs/loop-spec/PREREQUISITES.md:436` |

## External Services

| Service | Purpose | Source |
|---|---|---|
| Anthropic model API | Consumed transparently through the `claude`/`pi`/`opencode` CLIs — loop-spec makes no direct HTTP calls to it | `lib/harness.sh:103-118` |
| GitHub (`gh` CLI + REST/GraphQL via `gh api`) | PR creation, listing, review-comment/decision fetch, readiness toggling for the deliver phase; credential host defaults to `github.com` / `GH_HOST`, with `GH_ENTERPRISE_TOKEN` / `GITHUB_ENTERPRISE_TOKEN` support for GitHub Enterprise | `lib/pr-comments.sh:157,201-227`; `lib/pr-delivery.sh:311-491`; `lib/credential-refresh.sh:59-77` |

No databases, message queues, or other third-party HTTP APIs are called directly by the plugin.
