# PR 100 audit

For the maintainer deciding whether to merge
[aztechead/loop-spec#100](https://github.com/aztechead/loop-spec/pull/100)
(`fix/6.6.4-fastapi-live-run`). After this page you can accept, reject, or
request the listed residuals. Line numbers are
[`f5bec2c`](https://github.com/aztechead/loop-spec/commit/f5bec2ce1745b4d884b645e18323e6590b550f4a)
unless a historical cite names an earlier SHA.

First pass (against `b981a2a`): four FastAPI oneshot defects were real and
pinned; three high findings were not. Five follow-up commits took that merge
bar. `f5bec2c` is a 68-script `set -euo pipefail` sweep that grew out of the
`execute-prepare` leftover, not out of the FastAPI run.

**Verdict.** Mergeable if you accept that #100 is now the FastAPI fixes **and**
one shell mode for `lib/` and `hooks/`. The FastAPI defects and the audit
remediations still hold. The conversion found real `-e` bugs and pins the
mode. What remains is policy (oneshot extras) and a leftover they already
named (33 scripts that were already `-euo` still have a bare `shift 2`).

## Status of the first-pass findings

Unchanged from `7f1ad22`. High findings stay closed. `execute-prepare.sh` is
`set -euo pipefail`; python-path’s changelog names asdf and mise. Oneshot
extras are still a reviewer finding (`2f64699`).

## `f5bec2c` — one shell mode

Claim vs tree: every shipped `*.sh` under `lib/` and `hooks/` (not
`*.test.sh`) opens with `set -euo pipefail`, or `set -uo pipefail` plus a
same-line reason. None uses the relaxation.
The new coverage suite
([`f5bec2c`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/tests/shell-mode-coverage.test.sh))
is registered in `tests/run-all.sh`.
[`skills/shared/human-code.md`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/skills/shared/human-code.md)
states the rule under “fail loudly”. 208 scripts passed the new suite here.

Bugs the conversion had to fix to turn `-e` on, and that the PR body names:

- [`oneshot.sh` `footprint_shape`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/lib/graph/probes/oneshot.sh):
  `[[ "$p" == /* ]] && full ...` left the function’s status at 1 on every
  relative path; under `-e` the probe aborted. `return 0` is the fix.
  Relative-path oneshot cases in `tests/lib/graph-probes.test.sh` would have
  gone red without it (141 pass here). No case names this last-command trap
  by itself.
- [`prepare-environment.sh`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/lib/prepare-environment.sh):
  `rc=$?` after the watchdog ran after `-e` would already have exited.
  `|| rc=$?` makes exit 10 / 12 reachable. `tests/lib/prepare-environment.test.sh`
  covers “setup command failure has a distinct exit” (45 pass).
- [`deliver.sh`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/lib/deliver.sh)
  / [`pr-delivery.sh`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/lib/pr-delivery.sh):
  `jq | tail` / `ls-remote | cut` under `pipefail` aborted instead of
  treating empty as “no answer.” `|| true` keeps the fail-safe empty. The
  FastAPI `post_gate_drift` / `pushed-no-pr` cases still pass (`deliver` 152,
  `pr-delivery` 161, `checkpoint-pr` 44). The no-`gh` fixture now ships
  `dirname` so `. credential-refresh.sh` does not abort under `-e`.
- Trailing flag with no value: converted scripts `shift 2 || exit 2` instead
  of looping. At-end EXECUTE squash logs on stderr when it leaves task
  commits in place instead of failing the gate
  ([`execute-exit-gate.sh`](https://github.com/aztechead/loop-spec/blob/f5bec2ce1745b4d884b645e18323e6590b550f4a/lib/execute-exit-gate.sh)).

The sweep matches house style. It is also 73 files on a FastAPI live-run PR.
That is a review-cost fact, not a defect in the conversion.

## Residual

1. Oneshot extras are judgment (`2f64699`). A later live run that ships a
   fourth authored file because the reviewer called it scaffold or Minor is
   this policy, not a missed patch.
2. 33 scripts that were already `-euo` still have a bare `shift 2` (a
   valueless trailing flag exits 1 with no usage line). The PR body names
   this; `f5bec2c` did not touch them.
3. The oneshot `footprint_shape` last-command trap is pinned only by the
   existing relative-path probe cases, not by a test that says so.

Checked at this head: `shell-mode-coverage` (208), `graph-probes` (141),
`oneshot-exit-gate`, `python-path` (14), `human-code-coverage` (95),
`deliver` (152), `prepare-environment` (45), `pr-delivery` (161),
`checkpoint-pr` (44). Full `tests/run-all.sh` was not re-run here.

## Harness API

Unchanged from `7f1ad22`. EXECUTE still omits `subagent_type` on every
harness; the `agent-type` probe is gone.

## Suggested merge bar

Satisfied for the FastAPI fixes and the audit remediations. Merge `f5bec2c`
with them only if you want one shell mode in the same PR; otherwise split
the sweep. Understand the oneshot scope move: the gate no longer counts
files; the reviewer does.
