# PR 100 audit

For the maintainer deciding whether to merge
[aztechead/loop-spec#100](https://github.com/aztechead/loop-spec/pull/100)
(`fix/6.6.4-fastapi-live-run`). After this page you can accept, reject, or
request the listed residuals. Line numbers are
[`e420cf5`](https://github.com/aztechead/loop-spec/commit/e420cf5c99bdf43db91def255b0e0c834f22e56d)
unless a historical cite names an earlier SHA.

First pass (against `b981a2a`): four FastAPI oneshot defects were real and
pinned; three high findings were not (snapshot NOTE on stdout, Codex
`agent-type` fighting the EXECUTE stanza, two red suites deleted). Four
follow-up commits (`6c41549`, `12631fc`, `2f64699`, `e420cf5`) took the
audit’s merge bar and then the callouts on that bar.

**Verdict.** Mergeable. The original four FastAPI fixes still hold. The three
high findings are closed in code and now have behavioral pins where the
re-audit asked for them. What remains is policy (oneshot extras are a
reviewer finding) and changelog leftovers, not open defects.

#100’s body lists remediations through `2f64699` and still says the quoted
`route: "full"` fix is the lint only. `e420cf5` is the driver half of that
fix plus the snapshot behavior case. The body lags the tree.

## Status of the first-pass findings

| Finding | First pass | At `e420cf5` |
|---|---|---|
| `NOTE [snapshot]` / `NOTE [escalate]` on stdout hid `NEXT` | High | Closed. Both print to stderr ([snapshot](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/lib/graph/driver.py#L1273-L1278), [escalate](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/lib/graph/driver.py#L1363)). [`cycle-driver.test.sh`](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/tests/lib/cycle-driver.test.sh#L780-L801) drives a plugin copy whose source moves mid-phase: first stdout line is `PAUSED`, no `NOTE` on stdout, note on stderr and in `warnings`, no escalation. The oneshot deadlock path already pinned the escalate note the same way ([L656–L659](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/tests/lib/cycle-driver.test.sh#L656-L659)) |
| Codex `agent-type` vs “DEFAULT agent so charters do not apply” | High | Closed by removal. [`execute-subagent.md`](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/skills/shared/execute-subagent.md#L272-L286) omits `subagent_type` on every harness; Codex `spawn_agent` carries no `agent_type` on this rung; ADK maps an omitted type to `implementer` / `spec-compliance-reviewer` |
| Deleted `cycle-driver` / `execute-prepare` suites | High | Closed. Both registered again. The new snapshot and quoted-route cases live in the restored driver suite (294 pass at this head) |
| `oneshot-artifact-budget` deleted as an “eval consumer” | Quality | Closed. Restored under `tests/fixtures/oneshot-artifact-budget/` |
| Hostless checkpoint error code `gh_missing` | Sibling | Closed. Checkpoint/observe refuse as `remote_hostless` |
| `execute-prepare` commit failure reported previous HEAD | Sibling | Closed. `add && commit` failure exits 2. Workspace skip is now named as design ([`execute-prepare.sh`](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/lib/execute-prepare.sh#L64-L66)): artifacts sit at the workspace root, outside every repo’s `integrate-task`. The script is still `set -uo` without `-e` |
| Workspace DELIVER had no `post_gate_drift` | Sibling | Closed. `repoHeadShas` on `phase_end`; DELIVER refuses per repo |
| Lockfile map missed Pipfile / nvm / ruby pins | Sibling | Superseded. Fourth-file gate and allowlist are gone (`2f64699`) |
| `uv init` `README.md` / `main.py` as next fourth file | Sibling | Superseded by the same policy change |
| `oneshot-spec-lint` rejected quoted `route: "full"` | Sibling | Closed in the lint, the probe, and the driver. [`route_is_full`](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/lib/graph/driver.py#L2149-L2153) is the four former unquoted matches; [`spec_escalate`](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/tests/lib/cycle-driver.test.sh#L803-L813) on a quoted frontmatter keeps one `route:` line |
| python-path `/tmp` plant; pyenv-only | Quality | Closed in code (0700, owner check, asdf/mise). CHANGELOG 6.6.4 **Changed** still describes the probe as pyenv-only; the audit **Fixed** bullets name asdf/mise |
| Oneshot dispatch event after the Agent returned | Quality | Closed. Dispatch is recorded before the Agent call |
| `add-files` does not widen a running brief | Sibling | Closed as documentation. Rework is a new dispatch |
| CHANGELOG mashed `.gitignore` / python-path bullets | Quality | Closed as separate bullets. The pyenv-only Changed sentence is the leftover |

The four FastAPI defects from the PR body are still the right fixes: hostless
remote is `pushed-no-pr`, `headSha` / `post_gate_drift` bind the candidate,
the reviewer cite goes through `{probe_dir}`, and extras left uncommitted still
block DELIVER as dirt.

## Oneshot policy change (`2f64699`)

The gate no longer escalates a diff file the footprint does not name, and
`generated_by_manifest` is gone. The footprint still selects the route (at most
three named files, each must appear in the diff). A file outside that list is
the reviewer’s finding: **Important** if the change authored it (blocks),
**Critical** with `route: full` when authored files outgrew the footprint,
mention-only for scaffolder output beside a manifest
([`agents/code-reviewer.md`](https://github.com/aztechead/loop-spec/blob/e420cf5c99bdf43db91def255b0e0c834f22e56d/agents/code-reviewer.md#L39)).
The oneshot skill commits everything the change wrote.

The original FastAPI failure mode (lockfiles committed **inside DELIVER**,
named as `verifiedSha`) is still closed by `post_gate_drift` and
`dirty_worktree`. What is no longer a gate is an extra file committed
**before** the gate on a still-oneshot route.

## Residual

1. Oneshot extras are judgment. If a later live run ships a fourth authored
   file because the reviewer called it scaffold or Minor, that is this policy,
   not a missed patch.
2. CHANGELOG 6.6.4 **Changed** still introduces `python-path.sh` as a pyenv
   shim. The later **Fixed** bullet is the accurate one.
3. `execute-prepare.sh` is still `set -uo` without `-e`. The commit-failure
   path is an explicit `|| exit 2`; this is house style, not the stale-HEAD
   bug.

Checked at this head: `tests/live-run-findings-coverage.test.sh` (56 pass)
and `tests/lib/cycle-driver.test.sh` (294 pass), including the new moving-plugin
and quoted-route cases. Full `tests/run-all.sh` was not re-run here; #100’s
test plan still claims 243 suites.

## Harness API at this head

| Harness | EXECUTE implementer/reviewer |
|---|---|
| Claude Code | Omit `subagent_type` (default Agent, self-contained prompt) |
| OpenCode | Tool requires a type; maps omitted EXECUTE dispatches to `loop-spec-implementer` / `loop-spec-spec-compliance-reviewer` |
| ADK | Same mapping, written: `implementer` / `spec-compliance-reviewer` |
| Codex | `spawn_agent` with no `agent_type`; role TOMLs serve phase-skill dispatches that name a role |

That matches the EXECUTE stanza. The `agent-type` probe is gone.

## Suggested merge bar

Satisfied. Merge for the four FastAPI mechanical fixes, the three high
remediations, and `e420cf5` (quoted `route: full` in the driver, snapshot
NOTE as a behavior case). Understand the oneshot scope move: the gate no
longer counts files; the reviewer does.
