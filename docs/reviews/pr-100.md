# PR 100 audit

For the maintainer deciding whether to merge
[aztechead/loop-spec#100](https://github.com/aztechead/loop-spec/pull/100)
(`fix/6.6.4-fastapi-live-run`). After this page you can accept, reject, or
request the listed residual. Line numbers are
[`7f1ad22`](https://github.com/aztechead/loop-spec/commit/7f1ad220591223452e96a3fb2846839be96aea16)
unless a historical cite names an earlier SHA.

First pass (against `b981a2a`): four FastAPI oneshot defects were real and
pinned; three high findings were not (snapshot NOTE on stdout, Codex
`agent-type` fighting the EXECUTE stanza, two red suites deleted). Five
follow-up commits (`6c41549`, `12631fc`, `2f64699`, `e420cf5`, `7f1ad22`)
took the audit’s merge bar and then the callouts on that bar.

**Verdict.** Mergeable. The original four FastAPI fixes still hold. The three
high findings are closed in code and have behavioral pins. `7f1ad22` closed
the two leftover callouts (CHANGELOG python-path still saying pyenv-only;
`execute-prepare.sh` without `-e`). What remains is policy: oneshot extras are
a reviewer finding.

#100’s body now lists remediations through `7f1ad22`. That matches this head.

## Status of the first-pass findings

| Finding | First pass | At `7f1ad22` |
|---|---|---|
| `NOTE [snapshot]` / `NOTE [escalate]` on stdout hid `NEXT` | High | Closed. Both print to stderr. [`cycle-driver.test.sh`](https://github.com/aztechead/loop-spec/blob/7f1ad220591223452e96a3fb2846839be96aea16/tests/lib/cycle-driver.test.sh#L780-L801) drives a plugin copy whose source moves mid-phase: first stdout line is `PAUSED`, no `NOTE` on stdout. The oneshot deadlock path pins the escalate note the same way |
| Codex `agent-type` vs “DEFAULT agent so charters do not apply” | High | Closed by removal. EXECUTE omits `subagent_type` on every harness; Codex `spawn_agent` carries no `agent_type` on this rung; ADK maps an omitted type to `implementer` / `spec-compliance-reviewer` |
| Deleted `cycle-driver` / `execute-prepare` suites | High | Closed. Both registered again |
| `oneshot-artifact-budget` deleted as an “eval consumer” | Quality | Closed. Restored under `tests/fixtures/oneshot-artifact-budget/` |
| Hostless checkpoint error code `gh_missing` | Sibling | Closed. Checkpoint/observe refuse as `remote_hostless` |
| `execute-prepare` commit failure reported previous HEAD | Sibling | Closed. `add && commit` failure exits 2. Workspace skip is design ([comment](https://github.com/aztechead/loop-spec/blob/7f1ad220591223452e96a3fb2846839be96aea16/lib/execute-prepare.sh#L64-L66)). [`set -euo pipefail`](https://github.com/aztechead/loop-spec/blob/7f1ad220591223452e96a3fb2846839be96aea16/lib/execute-prepare.sh#L37) as of `7f1ad22`. `tests/lib/execute-prepare.test.sh`: 90 pass at this head |
| Workspace DELIVER had no `post_gate_drift` | Sibling | Closed. `repoHeadShas` on `phase_end`; DELIVER refuses per repo |
| Lockfile map missed Pipfile / nvm / ruby pins | Sibling | Superseded. Fourth-file gate and allowlist are gone (`2f64699`) |
| `uv init` `README.md` / `main.py` as next fourth file | Sibling | Superseded by the same policy change |
| `oneshot-spec-lint` rejected quoted `route: "full"` | Sibling | Closed in the lint, the probe, and `route_is_full`. `spec_escalate` on a quoted frontmatter keeps one `route:` line |
| python-path `/tmp` plant; pyenv-only | Quality | Closed in code and changelog. **Changed** now names pyenv, asdf, and mise, the 0700 directory, and the planted-symlink refusal ([`CHANGELOG.md`](https://github.com/aztechead/loop-spec/blob/7f1ad220591223452e96a3fb2846839be96aea16/CHANGELOG.md#L49-L55)) |
| Oneshot dispatch event after the Agent returned | Quality | Closed. Dispatch is recorded before the Agent call |
| `add-files` does not widen a running brief | Sibling | Closed as documentation. Rework is a new dispatch |
| CHANGELOG mashed `.gitignore` / python-path bullets | Quality | Closed |

The four FastAPI defects from the PR body are still the right fixes: hostless
remote is `pushed-no-pr`, `headSha` / `post_gate_drift` bind the candidate,
the reviewer cite goes through `{probe_dir}`, and extras left uncommitted still
block DELIVER as dirt.

## Oneshot policy change (`2f64699`)

The gate no longer escalates a diff file the footprint does not name, and
`generated_by_manifest` is gone. The footprint still selects the route (at most
three named files, each must appear in the diff). A file outside that list is
the reviewer’s finding: **Important** if the change authored it (blocks),
**Critical** with `route: full` when authored files outgrew the footprint,
mention-only for scaffolder output beside a manifest
([`agents/code-reviewer.md`](https://github.com/aztechead/loop-spec/blob/7f1ad220591223452e96a3fb2846839be96aea16/agents/code-reviewer.md#L39)).
The oneshot skill commits everything the change wrote.

The original FastAPI failure mode (lockfiles committed **inside DELIVER**,
named as `verifiedSha`) is still closed by `post_gate_drift` and
`dirty_worktree`. What is no longer a gate is an extra file committed
**before** the gate on a still-oneshot route.

## Residual

Oneshot extras are judgment. If a later live run ships a fourth authored file
because the reviewer called it scaffold or Minor, that is this policy, not a
missed patch.

Checked at this head: `tests/lib/execute-prepare.test.sh` (90 pass) after
`set -euo pipefail`. Full `tests/run-all.sh` was not re-run here; #100’s test
plan still claims 243 suites.

## Harness API at this head

| Harness | EXECUTE implementer/reviewer |
|---|---|
| Claude Code | Omit `subagent_type` (default Agent, self-contained prompt) |
| OpenCode | Tool requires a type; maps omitted EXECUTE dispatches to `loop-spec-implementer` / `loop-spec-spec-compliance-reviewer` |
| ADK | Same mapping, written: `implementer` / `spec-compliance-reviewer` |
| Codex | `spawn_agent` with no `agent_type`; role TOMLs serve phase-skill dispatches that name a role |

That matches the EXECUTE stanza. The `agent-type` probe is gone.

## Suggested merge bar

Satisfied. Merge for the four FastAPI mechanical fixes and the remediations
through `7f1ad22`. Understand the oneshot scope move: the gate no longer
counts files; the reviewer does.
