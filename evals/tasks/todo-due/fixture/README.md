# todo

`python3 -m todo.cli add "title"`, `python3 -m todo.cli list`, `python3 -m todo.cli done ID`.
Items live in `todo.json` (or `--file PATH` / `TODO_FILE`).
Tests: `python3 -m unittest discover -s tests`.
