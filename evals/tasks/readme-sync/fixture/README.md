# backup

Copies a directory tree.

## Usage

```
python3 backup.py --source DIR --dest DIR [--compress]
```

- `--source DIR`: directory to copy from
- `--dest DIR`: directory to copy into
- `--compress`: gzip each file after copying
